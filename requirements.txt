# GUI
PyQt5>=5.15

# Automation engine
PyAutoGUI>=0.9.54

# Image processing / recognition
numpy>=1.24
Pillow>=10.0

# Global keyboard / mouse hotkeys
keyboard>=0.13.5

# Windows low-level hooks & window control (Windows only)
pywin32>=306 ; platform_system == "Windows"

# Optional: faster & more accurate template matching if installed
# opencv-python>=4.8
